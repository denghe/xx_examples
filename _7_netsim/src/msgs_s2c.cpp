#include "pch.h"
#include "looper.h"
#include "msgs.h"

namespace Msgs {
	namespace S2C {	// id == 2000 ~ 2999

		int32_t Join_r::ReadFrom(xx::Data_r& dr) {
			assert(false);
			return 0;
		}

		void Join_r::WriteTo(xx::Data& d) const {
			assert(false);
		}

	}
}
